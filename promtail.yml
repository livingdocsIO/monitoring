server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /data/promtail/positions.yaml

clients:
  - url: http://loki:3100/api/prom/push

scrape_configs:
- job_name: system
  static_configs:
  - targets:
      - localhost
    labels:
      job: varlogs
      __path__: /var/log/*log

- job_name: events
  static_configs:
  - targets:
      - localhost
    labels:
      job: events
      __path__: /Users/marcbachmann/.dotfiles/.hammerspoon/event.log
